requests>=2.31
beautifulsoup4>=4.12
openpyxl>=3.1
playwright>=1.40  # run `playwright install chromium` once after installing
pypdf>=4.0
openai>=1.0  # only for journal_scrapers/llm_extract/client.py -- HTTP client shape for any OpenAI-compatible server, incl. vLLM; not needed for the rest of the project
trafilatura>=1.8  # only for journal_scrapers/llm_extract/clean_text.py -- HTML to clean Markdown
